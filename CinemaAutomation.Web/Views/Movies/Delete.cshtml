@model CinemaAutomation.Web.Data.Entities.Movie
@* View model representing the movie to be deleted *@

@{
    ViewData["Title"] = "Delete Movie"; // Sets the page title
}

<style>
    .page-title {
        font-weight: 900;
        color: #7f1d1d;
        margin-bottom: 6px;
        letter-spacing: 0.4px;
        text-align: center;
    }

    .page-sub {
        color: #6b7280;
        margin-bottom: 28px;
        font-size: 0.95rem;
        text-align: center;
    }

    .delete-wrapper {
        display: flex;
        justify-content: center;
    }

    .delete-card {
        background: #ffffff;
        border-radius: 24px;
        padding: 32px;
        box-shadow: 0 16px 40px rgba(0,0,0,0.14);
        max-width: 620px;
        width: 100%;
    }

    .warning-box {
        background: #fef2f2;
        border-left: 6px solid #dc2626;
        border-radius: 14px;
        padding: 14px 18px;
        color: #7f1d1d;
        font-weight: 700;
        margin-bottom: 22px;
        text-align: center;
    }

    dl.row dt {
        font-weight: 700;
        color: #374151;
    }

    dl.row dd {
        color: #111827;
        margin-bottom: 10px;
    }

    .action-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 28px;
        flex-wrap: wrap;
        gap: 12px;
    }

    .delete-btn {
        border-radius: 18px;
        padding: 12px 30px;
        font-weight: 800;
        font-size: 0.95rem;
        border: none;
        background: linear-gradient(135deg, #dc2626, #b91c1c);
        color: #ffffff;
        letter-spacing: 0.4px;
        transition: all 0.25s ease;
    }

        .delete-btn:hover {
            background: linear-gradient(135deg, #b91c1c, #991b1b);
            transform: translateY(-2px);
            box-shadow: 0 14px 30px rgba(220, 38, 38, 0.45);
        }

        .delete-btn:active {
            transform: translateY(0);
            box-shadow: 0 8px 18px rgba(220, 38, 38, 0.35);
        }

    .back-link {
        font-weight: 600;
        color: #2563eb;
        text-decoration: none;
    }

        .back-link:hover {
            text-decoration: underline;
        }
</style>

<h2 class="page-title">Delete Movie</h2>
<p class="page-sub">This action cannot be undone</p>

<div class="delete-wrapper">
    <div class="delete-card">

        <div class="warning-box">
            Are you sure you want to permanently delete this movie?
        </div>

        <dl class="row">
            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Title)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.Title)</dd>
            @* Displays movie title *@

            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Description)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.Description)</dd>
            @* Displays movie description *@

            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.DurationMinutes)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.DurationMinutes)</dd>
            @* Displays movie duration *@

            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.PosterPath)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.PosterPath)</dd>
            @* Displays stored poster path *@

            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.IsActive)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.IsActive)</dd>
            @* Displays movie active/passive status *@

            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.CreatedAt)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.CreatedAt)</dd>
            @* Displays movie creation date *@
        </dl>

        @* Form submits delete confirmation to Delete action *@
        <form asp-action="Delete">
            <input type="hidden" asp-for="MovieId" />
            @* Sends movie ID for deletion *@

            <div class="action-row">
                <input type="submit"
                       value="Delete Movie"
                       class="delete-btn" />
                @* Confirms movie deletion *@

                <a asp-action="Index" class="back-link">
                    Cancel and go back
                </a>
                @* Navigates back to movie list without deleting *@
            </div>
        </form>

    </div>
</div>

